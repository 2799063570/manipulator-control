---
title: Day 10 - 图搜索与 TF
tags: [14天打卡, 图, DFS, BFS, TF]
---

# Day 10：图搜索与 TF

关联计划：[[33-基础补齐-14天执行清单]]

## 当日任务

- 算法：岛屿数量（DFS/BFS）、课程表（拓扑排序概念）。
- C++：`mutex`、`lock_guard`、`unique_lock` 的区别。
- ROS：运行 TF 示例或项目，使用 `ros2 run tf2_tools view_frames` 生成 TF 图。

- 算法：岛屿数量（DFS/BFS）、课程表（拓扑排序概念）。



mutex是底层互斥锁，用于保护共享资源，防止多个线程同时访问。
lock_guard是mutex的RAII封装，构造时自动加锁，析构时自动释放，简单安全，适合简单临界区。
unique_lock也是RAII锁，但是更加灵活，支持延迟加锁、手动unlock、锁转移，并且可以配合condition_variable使用。

一般简单同步使用lock_guard，需要条件变量或者复杂锁控制时使用unique_lock。



# 题目1：岛屿数量（LeetCode 200）

## 题目描述

给你一个二维网格 `grid`：

- `'1'` 表示陆地
- `'0'` 表示水

一个岛屿由上下左右相连的陆地组成。

求：

> 网格中岛屿的数量。

```cpp
void dfs(vector<vector<int>>& grid, int i, int j)
{
    int m = grid.size();
    int n = grid[0].size();
    // 越界或者不是陆地
    if(i < 0 || i >= m ||
       j < 0 || j >= n ||
       grid[i][j] == 0)
    {
        return;
    }
    if (grid[i][j] == 1) 
    {
        grid[i][j] = 0;
        dfs(grid, i-1, j);
        dfs(grid, i, j-1);
        dfs(grid, i+1, j);
        dfs(grid, i, j+1);
    }
}
```

```cpp
void bfs(
    vector<vector<int>>& grid,
    int i,
    int j)
{
    int m = grid.size();
    int n = grid[0].size();

    queue<pair<int,int>> q;

    q.push({i,j});

    // 入队立即标记
    grid[i][j]=0;
    int dirs[4][2]={
        {-1,0},
        {1,0},
        {0,-1},
        {0,1}
    };

    while(!q.empty())
    {
        auto node=q.front();
        q.pop();

        for(auto& dir:dirs)
        {
            int ni=node.first+dir[0];
            int nj=node.second+dir[1];
            if(ni>=0 && ni<m &&
               nj>=0 && nj<n &&
               grid[ni][nj]==1)
            {
                grid[ni][nj]=0;
                q.push({ni,nj});
            }
        }
    }
}
```

```cpp
int getIslandsNum(vector<vector<int>>& grid)
{
    int ret = 0;
    for (int i = 0, size_i = grid.size(), size_j = grid[0].size(); i<size_i; i++)
    {
        for (int j = 0; j<size_j; j++)
        {
            if (grid[i][j] == 1)
            {
                dfs(grid, i, j);
                ret++;
            }
        }
    }
    return ret;
}
```

# 题目2：课程表（LeetCode 207）

## 题目描述

你需要修完 `numCourses` 门课程。

课程编号：

```
0 ~ numCourses-1
```



## 算法记录

- 日期：
- 分类：哈希 / 双指针 / 二分 / 栈队列 / 链表 / 树图 / 堆 / DP
- 限时：__ 分钟；实际：__ 分钟；是否独立完成：是 / 否
- 最终复杂度：时间 __；空间 __
- 边界测试：空输入 / 单元素 / 重复值 / 极值 / 其他
- 错误类型：知识缺口 / 思路 / 实现 bug / 边界 / 时间管理
- 一句话根因：
- 二刷日期：明天、7 天后

## C++ / ROS 记录

- 三种锁的使用场景：
- TF 生成命令：
- frame 父子关系：
- 一个 child frame 为什么不能有两个 parent：

